name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: Swift test
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Test
        run: swift test
